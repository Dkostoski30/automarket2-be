apiVersion: apps/v1
kind: Deployment
metadata:
  name: inquiry-service
  namespace: automarket
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inquiry-service
  template:
    metadata:
      labels:
        app: inquiry-service
    spec:
      containers:
        - name: inquiry-service
          image: automarket/automarket-inquiry-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: automarket-config
            - secretRef:
                name: automarket-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "210Mi"
