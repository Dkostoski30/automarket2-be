<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.automarket</groupId>
        <artifactId>automarket-platform</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>automarket-messaging</artifactId>
    <name>AutoMarket Messaging</name>
    <description>Transactional outbox and consumer inbox for reliable Kafka messaging</description>

    <dependencies>
        <!-- Event envelope + topic names -->
        <dependency>
            <groupId>com.automarket</groupId>
            <artifactId>automarket-events</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Outbox and inbox are JPA tables written in the caller's transaction -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>

        <!-- Gauges for outbox depth and staleness. Optional: a service that does
             not run actuator still compiles, the metrics just go nowhere. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
